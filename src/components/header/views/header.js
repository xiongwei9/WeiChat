import React from 'react';

const Header = () => {
    const titleStyle = {
        textAlign: 'center',
    };

    return (
        <header>
            <h1 style={titleStyle}>Hello</h1>
        </header>
    );
};

export default Header;